# Operator restructure — 2026-09-01

Issued after the full-house review (main operator session, 2026-08-30 → 09-01):
the September Gauntlet hunt, the Hermes A/B calibration read, the Midas ghost
autopsy, and the grading-calendar audit. The pantheon's SHAPE is confirmed —
four live gods, four distinct engines. These are flows, fixes, and clocks, each
with its shrink branch pre-committed IN WRITING BEFORE the data arrives.

The machine-readable copy the gods act on is
`cache/shared_operator_directives.json` (state branch `claude/live`); each god's
command file now points at it. This doc is the human record.

| # | Directive | Status |
|---|-----------|--------|
| D1 | Hermes: ALOT ledger backfill authorized per his own 08-31 diagnosis; he applies it himself, reconcile clean → entries resume | active |
| D2 | Hermes funding pre-commitment: APGE+FSEA both close clean by ~09-30 → fund to ~$7k; either breaks → hold $4k, study the stop | active |
| D3 | Midas program closure (scan+ghost+A/B) on grounds: 0-for-3 thesis, −2.4%/wk finalist pool, 7 weeks unable to produce a head-to-head | PENDING SIGNATURE |
| D4 | Oracle: no new round until sector fields + cluster-cap wiring are real (his own top precondition, made binding); no new capital | active |
| D5 | Plutus October pre-commitment: deluxe lags control → cut deluxe, no relitigating; holds + v6 intersection keeps winning → intersection-N25 is a January spec-change candidate | active |
| D6 | Lab throughput: every /lab session ticks every open forward book; Proteus invited to host the two tender-slug forward books in his daily sweep | active |

Context safeguards added the same day (main session, not god-side): daily
operator-book + gods tracking routine (weekdays 20:10 UTC, self-bound), the
Pantheon Ledger artifact (per-position total return + SPY excess, refreshed
daily), a monthly aggregate-exposure section (the account is ~95% long US
equity across five books in one account — that risk now has an owner), and a
09-30 one-shot checkpoint for D2.

Explicitly NOT done, on discipline: no new god (nothing lab-validated), no
early concentration of Plutus or Oracle (mid-experiment), no shrink toward the
floor (the mandate's shrink trigger is a failed checkpoint; the checkpoints are
weeks away and early lift evidence is positive).
