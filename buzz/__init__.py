"""Buzz — the weekly retail-acceleration machine (candidate replacement for Midas).

Thesis: not the most-talked-about stocks (that level is fully arbitraged), but
the small/mid-caps where talk is ACCELERATING — the second derivative of
attention — confirmed by real price/volume so it's organic money and not a pump.

DISCONNECTED and PAPER-FIRST. Shares no state with the four gods. It never
places a live order in v1: it produces a ranked shortlist + LLM recommendation
for a human, and a ghost validates it before any capital. If it earns its keep
against the ghost, it can inherit the Midas name and sleeve.

Pipeline: buzz acceleration (discovery) -> price/volume confirmation
(direction + anti-manipulation) -> small/mid filter -> basket -> LLM judges
authenticity/direction and recommends -> human decides.
"""
