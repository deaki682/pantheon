<resources>
  <style name="Theme.Realism" parent="Theme.AppCompat.NoActionBar">
    <item name="android:windowBackground">@android:color/black</item>
    <item name="android:statusBarColor">@android:color/black</item>
    <item name="android:navigationBarColor">@android:color/black</item>
    <!-- android 12+ draws a system splash screen before the window; a
         black backdrop makes launch read as one seamless surface instead
         of white-flash -> icon -> app -->
    <item name="android:windowSplashScreenBackground">@android:color/black</item>
  </style>
</resources>
